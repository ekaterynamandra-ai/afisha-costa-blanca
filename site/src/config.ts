// Site-wide constants. Change in one place.

// Telegram channel username (without @). When Kate renames the channel
// from afishaCB → kate_in_spain, just update the string below; every link
// across the site will pick it up.
export const TG_HANDLE = 'kate_in_spain';
export const TG_URL = `https://t.me/${TG_HANDLE}`;

export const SITE_EMAIL = 'ekaterynamandra@gmail.com';
